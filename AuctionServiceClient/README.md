# 📝 README.md


Это простой веб-клиент для работы с прикладным сервисом аукциона (**AuctionService**).  
Клиент написан на обычном HTML + JavaScript и выполняет запросы напрямую к API сервиса:  
- просмотр всех лотов  
- просмотр одного лота  
- создание лота  
- обновление лота  
- удаление лота  
- размещение ставки  

## 📦 Содержание проекта

```

├── src/index.html        # Основной веб-клиент
├── README.md

```

## 🚀 Запуск веб-клиента

Веб-клиент — это статические файлы. Чтобы он работал, его нужно запускать через HTTP-сервер.

### Вариант 1: Python (самый простой)

```bash
cd path/to/project
python3 -m http.server 8000
```

После запуска открой в браузере:

```
http://localhost:8000/index.html
```

### Вариант 2: Node.js (через serve)

```bash
npm install -g serve
serve .
```

### Вариант 3: VS Code

1. Установить расширение **Live Server**
2. Открыть `index.html` → ПКМ → **Open with Live Server**

---

## 🔧 Настройка API-адреса

Внутри `index.html` должен быть указан URL твоего AuctionService:

```js
const API_BASE = "http://localhost:8080"; // или адрес контейнера
```

Если AuctionService работает в Docker:

```js
const API_BASE = "http://auction-service:8080";
```

---

## 🧪 Доступные запросы клиента

### Получить список лотов

```
GET /lots
```

### Получить конкретный лот

```
GET /lots/{id}
```

### Создать лот (требуется токен оплаты)

```
POST /lots
Authorization: Bearer <token>
```

Тело:

```json
{
  "name": "Laptop ASUS",
  "start_price": 200,
  "auction_end_date": "2025-12-31T23:59:00"
}
```

### Обновить лот (оплачиваемый метод)

```
PUT /lots/{id}
Authorization: Bearer <token>
```

### Удалить лот (оплачиваемый метод)

```
DELETE /lots/{id}
Authorization: Bearer <token>
```

### Сделать ставку (оплачиваемый метод)

```
POST /lots/{id}/bid
Authorization: Bearer <token>
```

Тело:

```json
{
  "bid_amount": 310
}
```

---

## 🔐 Оплата и токены

Для следующих методов клиент **обязан** отправлять токен:

* `CreateLot`
* `UpdateLot`
* `DeleteLot`
* `PlaceBid`

Токен клиент получает через PaymentService (API оплаты);
затем отправляет его в заголовке:

```
Authorization: Bearer <token>
```

---

## 🛠 Требования

* Любой браузер
* HTTP-сервер (Python/Node/LiveServer)
* Запущенный AuctionService

---

## 🧭 Запуск всего проекта (коротко)

1. Запустить PaymentService и RegistryService
2. Запустить AuctionService
3. Узнать его порт и вписать в `index.html`
4. Запустить `python3 -m http.server`
5. Открыть веб-клиент в браузере

Готово ✨

